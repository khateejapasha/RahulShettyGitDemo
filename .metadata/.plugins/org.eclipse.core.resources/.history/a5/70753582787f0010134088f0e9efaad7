package FlipkartTest;

import java.sql.Driver;
import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.PageFactory;
import org.openqa.selenium.support.ui.ExpectedCondition;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.testng.annotations.Test;

import Flipkart.FlipkartHome;
import Flipkart.FlipkartProduct;
import groovyjarjarantlr4.v4.runtime.tree.xpath.XPath;
import io.cucumber.messages.types.Product;
import io.github.bonigarcia.wdm.WebDriverManager;

public class FlipkartTestClass extends FlipkartBase{

	@Test
	public void Initial()
	{
		FlipkartHome pf1 = PageFactory.initElements(driver,FlipkartHome.class);
		
		pf1.emailID();
		pf1.password();
		pf1.loginbtn();
	FlipkartProduct product=new FlipkartProduct(driver)	;	
	product.selectProduct();
	WebDriverWait wait=new WebDriverWait(driver, Duration.ofSeconds(10));
	wait.until(ExpectedConditions.visibilityOfElementLocated((By) driver.findElement(By.xpath("(//*[@class='btn w-10 rounded'])[1]"))));
	product.CartBtn();
			
			//driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(5));
			//product.validateText();
			
			//String child = driver.getWindowHandle();
			//driver.switchTo().window(child);
}
}
